{
  "rules": {
    ".read": false,
    ".write": false,

    "produk": {
      ".read": true,
      "$id": {
        ".write": "auth != null || (data.exists() && newData.exists() && newData.child('stok').isNumber() && data.child('stok').isNumber() && newData.child('stok').val() <= data.child('stok').val() && newData.child('nama').val() == data.child('nama').val() && newData.child('harga').val() == data.child('harga').val() && newData.child('gambar').val() == data.child('gambar').val())"
      }
    },

    "ulasan": {
      ".read": true,
      "$id": {
        ".write": "auth != null || (!data.exists() && newData.exists())"
      }
    },

    "berandaPosts": {
      ".read": true,
      ".write": "auth != null"
    },

    "notifikasiToko": {
      ".read": true,
      ".write": "auth != null"
    },

    "voucher": {
      ".read": true,
      "$code": {
        ".write": "auth != null"
      }
    },

    "pesanan": {
      "$id": {
        ".read": "auth != null || data.exists()",
        ".write": "auth != null || (!data.exists() && newData.exists()) || (data.exists() && newData.exists() && newData.child('invoice').val() == data.child('invoice').val() && newData.child('nama').val() == data.child('nama').val() && newData.child('whatsapp').val() == data.child('whatsapp').val() && newData.child('total').val() == data.child('total').val() && newData.child('waktu').val() == data.child('waktu').val() && newData.child('status').val() == data.child('status').val())"
      }
    },

    "pembayaran": {
      "$id": {
        ".read": "auth != null",
        ".write": "auth != null || (!data.exists() && newData.exists())"
      }
    },

    "pelacakan": {
      "$invoice": {
        ".read": true,
        ".write": "auth != null || (!data.exists() && newData.exists() && newData.child('invoice').val() == $invoice && (newData.child('status').val() == 'Menunggu Pembayaran' || newData.child('status').val() == 'Menunggu Konfirmasi COD'))"
      }
    },

    "pengembalian": {
      ".read": "auth != null",
      ".write": "auth != null"
    },

    "penarikan": {
      ".read": "auth != null",
      ".write": "auth != null"
    },

    "biteshipWebhookEvents": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
  }
}
